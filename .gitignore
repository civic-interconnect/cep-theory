# WHY: Prevents versioning files that are generated, temporary, or machine-specific
# WHY: Committing those files causes merge conflicts and inappropriately bloats repository history
# OBS: Sections organized: universal (all repos) then repo-specific
# REQ: Keep this file to avoid versioning inappropriate files

# === Universal across all repos ===

# WHY: OS-generated files
$RECYCLE.BIN/      # Windows recycle bin
._*                # macOS resource fork files
.AppleDouble       # macOS resource forks
.DS_Store          # macOS folder metadata
.LSOverride        # macOS icon positions
.Spotlight-V100/   # macOS search index
.Trashes           # macOS trash folder
Desktop.ini        # Windows folder config
ehthumbs.db        # Windows thumbnail cache
Icon\r             # macOS custom icon
Thumbs.db          # Windows thumbnails

# WHY: Editor-generated files
.history/          # VS Code local history
*~                 # Vim/Emacs backups
*.bak              # Backup files
*.backup           # Backup files
*.swo              # Vim swap files
*.swp              # Vim swap files
*.tmp              # Temporary files

# === Repo-specific: LaTeX theory papers ===

# WHY: Python virtual environments (if using scripts)
.venv/
__pycache__/

# WHY: LaTeX build artifacts - regenerated on each compile
*.aux              # Cross-references
*.bbl              # Bibliography
*.bcf              # Biblatex control
*.blg              # Bibliography log
*.fdb_latexmk      # latexmk database
*.fls              # File list
*.lol              # List of listings
*.log              # Compilation log
*.nav              # Beamer navigation
*.out              # PDF bookmarks
*.run.xml          # Biblatex run
*.snm              # Beamer notes
*.synctex.gz       # Editor sync data
*.toc              # Table of contents
*.vrb              # Beamer verbatim
*.xml              # Biblatex metadata

# WHY: Build directory and intermediate PDFs
build/
*.pdf
!00P1_cae_ontology.pdf
!00P2_cep_semantics.pdf
!00P3_cee_verticals.pdf

# WHY: Reference PDFs - available publicly, not redistributed here
# OBS: Helps locate sources without republishing copyrighted material
# CUSTOM: Update paths if reference locations change
paper01/refs/*.pdf
paper02/refs/*.pdf
paper03/refs/*.pdf
